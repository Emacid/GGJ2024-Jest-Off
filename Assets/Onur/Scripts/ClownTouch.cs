using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class ClownTouch : MonoBehaviour
{
    /*
    public bool LeftUp;
    public bool RightUp;
    public bool LeftDown;
    public bool RightDown;

    public GameObject winSFX;
    public GameObject splendidText;

    // Start is called before the first frame update
    void Start()
    {
        LeftDown = false; RightDown = false;
        RightUp = false; LeftUp = false;
    }

    // Update is called once per frame
    void Update()
    {
        if (LeftUp == true && RightUp == true && LeftDown == true && RightDown == true) 
        {
            winSFX.SetActive(true);
            splendidText.SetActive(true);
            Debug.Log("WIN!!!");
        }
    }

        void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("TouchPointLU")) 
        {
            //Debug.Log("Left Upper");
            LeftUp = true;
        }
        else if (other.CompareTag("TouchPointRU"))
        {
            //Debug.Log("Right Upper");
            RightUp = true;
        }
        else if (other.CompareTag("TouchPointLL"))
        {
            //Debug.Log("Lower Left");
            LeftDown = true;
        }
        else if (other.CompareTag("TouchPointLR"))
        {
            //Debug.Log("Lower Right");
            RightDown = true;
        }

    }

     void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("TouchPointLU")) 
        {
            //Debug.Log("Left Upper ");
            LeftUp = false;
        }
        else if (other.CompareTag("TouchPointRU"))
        {
            //Debug.Log("Right Upper");
            RightUp = false;
        }
        else if (other.CompareTag("TouchPointLL"))
        {
            //Debug.Log("Lower Left");
            LeftDown = false;
        }
        else if (other.CompareTag("TouchPointLR"))
        {
            //Debug.Log("Lower Right");
            RightDown = false;
        }
    }*/
}
